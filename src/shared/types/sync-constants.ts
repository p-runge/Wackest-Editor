// Fixed separation placed between source clusters that have no trustworthy temporal overlap
// with the rest of the timeline (a "hard cut" boundary) — see main/jobs/sync/graph.ts.
export const NO_OVERLAP_GAP_SEC = 5
